#! /bin/bash
echo $#  #Prints the no. of arguments passed.
echo $@ #Prints all the arguments at once but all the arguments are  as diff words.
echo $* #Prints all the arguments at once but as a single string. 

